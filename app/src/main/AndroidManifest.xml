<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ledger2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SubMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScheduleList" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Calendar" />
        <activity android:name=".AddList" />
        <activity android:name=".LedgerListActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".LedgerActivity" />
        <activity android:name=".MyPage" />
        <activity android:name=".MyPage_WishList" />
        <activity android:name=".MyPage_WishList_Add" />
    </application>

</manifest>